import numpy as np

x = np.array([1.0, 0.5])
mu = 0.1*np.sum(x)
log_var = 0.05*np.sum(x)
z = mu + np.exp(0.5*log_var)*0.2
reconstructed = z * np.array([0.6, 0.4])
print("Reconstructed output:", reconstructed)
