import numpy as np

x = np.array([0.2, 0.4, 0.6])
mu = np.sum(x)*0.1
log_var = np.sum(x)*0.05
z = mu + np.exp(0.5*log_var)*0.1
reconstructed = z * np.array([0.5, 0.3, 0.2])
print("Reconstructed output:", reconstructed)
