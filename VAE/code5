import numpy as np

x = np.array([0.3, 0.6, 0.9])
mu = 0.1*np.sum(x)
log_var = 0.05*np.sum(x)
z = mu + np.exp(0.5*log_var)*0.05
reconstructed = z * np.array([0.3, 0.2, 0.1])
print("Reconstructed output:", reconstructed)
